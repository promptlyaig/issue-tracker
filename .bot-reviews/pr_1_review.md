## 🤖 Automated PR Review

### Summary
This PR introduces filtering, pagination, and statistics calculation for issues in the issue tracker application. It also adds basic math utility functions in a separate module. The code introduces search/filter functionality and is generally well-structured, but contains a pagination bug.

### 🐛 Bugs Found

**[HIGH] Pagination Bug: Incorrect start index**
> The `_paginate` function calculates the start index incorrectly. `start = page * per_page` should be `start = (page - 1) * per_page` to correctly determine the beginning of the slice for a given page.
💡 *Change `start = page * per_page` to `start = (page - 1) * per_page` in the `_paginate` function.*

**[LOW] Missing error handling for page and per_page**
> Currently the application does not return a proper error if the page or per_page parameters provided by the user are incorrect
💡 *Return a 400 error if the page or per_page are not numbers. return a 422 error if either are zero.*

### ⚠️ Code Quality Issues

- Consider adding more comprehensive error handling, particularly around input validation (page and per_page values).
- The code lacks proper API endpoint to utilize the new features. Introduce the filter and pagination via a new API endpoint

### ✅ Positive Feedback

- The filtering logic is well-structured and easy to understand.
- The introduction of math utilities promotes code reusability.

### 🎫 Jira Tickets Created

- [PROM-326](https://promptlyai.atlassian.net/browse/PROM-326) — [HIGH] pagination_bug
- [PROM-327](https://promptlyai.atlassian.net/browse/PROM-327) — [LOW] input_validation

---
*Generated by PR Review Bot*